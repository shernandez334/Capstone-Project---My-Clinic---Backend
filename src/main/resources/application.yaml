server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: clinic-backend
  datasource:
    url: jdbc:mysql://localhost:3306/clinic_db?serverTimezone=UTC
    username: root
    password: XqmbwQJGPQQjydTXFeWWGectsBqLZfWe
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true